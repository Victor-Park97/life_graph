import 'package:flutter/widgets.dart';

class ShowLifePointPage extends StatelessWidget {
  const ShowLifePointPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}